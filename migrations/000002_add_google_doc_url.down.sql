-- =============================================================================
-- Откат ссылки на документ Google Docs.
--
-- Данные колонки при откате теряются: восстановить адрес можно только повторной
-- публикацией промпта (make task-1 google-publish <external_id>).
-- =============================================================================

ALTER TABLE article_outputs
    DROP COLUMN IF EXISTS google_doc_url;
