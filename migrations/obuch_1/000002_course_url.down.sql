-- Откат 000002: колонка адреса курса для кнопки призыва.
--
-- После отката статья без колонки не соберётся: поток obuch_1 требует адрес книгой.
-- Значения теряются безвозвратно, восстанавливаются повторным импортом.

ALTER TABLE article_inputs
    DROP COLUMN IF EXISTS course_url;
