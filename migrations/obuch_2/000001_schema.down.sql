-- =============================================================================
-- Снос схемы obuch_2 целиком. Применяется только к схеме obuch_2.
--
-- Необратимо: вместе с таблицами уходят и страницы, и их состояние. Входные данные
-- восстанавливает повторный import из книги, всё остальное — новый прогон. Пока задача
-- пуста, пересобрать её схему с нуля дешевле правки; появятся живые страницы — правило
-- станет прежним: новое изменение приезжает отдельным файлом, а не сносом схемы.
--
-- Порядок обратный созданию: дочерние таблицы раньше articles, иначе внешние ключи не дадут.
-- =============================================================================

DROP TABLE IF EXISTS article_errors;
DROP TABLE IF EXISTS article_outputs;
DROP TABLE IF EXISTS article_metadata;
DROP TABLE IF EXISTS article_research;
DROP TABLE IF EXISTS article_inputs;
DROP TABLE IF EXISTS articles;
