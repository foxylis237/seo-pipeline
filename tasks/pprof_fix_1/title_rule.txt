# Правило переименования: одна пара «было — стало» на всю пачку.
#
# Из пары выводится замена подстроки: общее начало и общий конец отбрасываются, изменившаяся
# середина расширяется до целых слов и становится правилом. Пример ниже даёт правило
# «внесением» → «практикой и внесением», и оно применяется к заголовку каждой статьи.
#
# Заголовок, в котором якоря нет, останавливает статью с внятной ошибкой — молча пропустить
# переименование нельзя. Уже переименованный заголовок остаётся как есть: повторный прогон
# не делает «с практикой и с практикой и».
#
# Проверить, что правило сработает на всей пачке, до единой правки в блоге:
#   make pprof-fix-1 run plan

было: Медсестра в санэпидконтроле - дистанционное обучение с внесением в ФИС ФРДО
стало: Медсестра в санэпидконтроле - дистанционное обучение с практикой и внесением в ФИС ФРДО
