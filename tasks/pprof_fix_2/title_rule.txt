# Правило переименования: одна пара «было — стало» на всю пачку.
#
# Из пары выводится замена подстроки: общее начало и общий конец отбрасываются, изменившаяся
# середина расширяется до целых слов и становится правилом. Пример ниже даёт правило
# «дистанционная» → «профессиональная», и оно применяется к заголовку каждой статьи, к её
# видимому заголовку (поле prof_title) и к SEO-заголовку, если подходит.
#
# Заголовок, в котором якоря нет, останавливает статью с внятной ошибкой — молча пропустить
# переименование нельзя. Так же остановится и заголовок в другой форме слова
# («дистанционное обучение»): правило выведено по женскому роду, и подгонять формы само оно
# не умеет. Уже переименованный заголовок остаётся как есть: повторный прогон не делает
# «профессиональная профессиональная».
#
# Проверить, что правило сработает на всей пачке, до единой правки в блоге:
#   make pprof-fix-2 run plan

было: Врач-хирург - дистанционная переподготовка с внесением в ФИС ФРДО
стало: Врач-хирург - профессиональная переподготовка с внесением в ФИС ФРДО
