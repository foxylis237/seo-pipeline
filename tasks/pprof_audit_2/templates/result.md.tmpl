{{/* Отчёт устроен так, чтобы из него было удобно копировать: значения и списки стоят
блоками, а не в таблице — таблицу приходится вычищать после вставки. Оценка первой строкой
намеренно: по ней страницы сравнивают между собой и решают, за какую браться. Сама эта
ремарка в отчёт не попадает. */}}# {{.Score}}

**{{.Title}}**

Запись {{.PostID}} ({{.PostType}}) · тема: {{.Topic}}
Обязательные поля: {{.RequiredFields}}
Вопросов в блоке FAQ: {{.FAQ}}
Баллы по критериям: {{.Scores}}

## Ссылка

```text
{{.URL}}
```

## Детальный разбор

```text
{{.Breakdown}}
```

## Критические ошибки

```text
{{.Critical}}
```

## Все найденные ошибки

```text
{{.Issues}}
```

## Не разобрано

```text
{{.Unparsed}}
```
