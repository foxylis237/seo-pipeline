В этом чате мы последовательно подготовим одну статью: структура, текст, редактура, информация для публикации и HTML. Каждый следующий шаг опирается на предыдущие сообщения, поэтому держи их в контексте и не теряй заголовки и формулировки.

Шаг 1 из 5 — структура.

Название статьи: {{.Title}}

Собери структуру статьи под информационный запрос, который выражен названием. Структура обязана закрывать вопрос читателя целиком и быть логичной — но не «максимально полной»: полнота набирается содержанием разделов, а не их количеством. Лишний заголовок не добавляет ответа, он растягивает статью и заставляет читателя листать.

ЛИМИТ ЗАГОЛОВКОВ
Статья — 9000–11000 знаков без пробелов, это примерно 1400–1800 слов. Заголовков в ней 20–30 вместе с H1:
- H2 — 6–9, считая два обязательных финальных раздела;
- H3 — 2–4 под одним H2, 18–25 на всю статью;
- H4 — только в обзорах и перечислениях, где под H3 разбирается несколько однотипных позиций. В остальных статьях его нет.

ПЛОТНОСТЬ ЗАГОЛОВКОВ
Заголовок идёт примерно каждые 50–90 слов. Кусок длиннее 300 слов без подзаголовка недопустим ни при каких условиях — это порог, за которым читаемость проваливается, но держаться у самого порога не надо.
На 1000 слов текста приходится 4–6 H2, и у каждого свои подзаголовки.
Стена текста без заголовка — место, где читатель уходит со страницы.

ДЛИНА ЗАГОЛОВКА
8–15 слов. Короче — заголовок не говорит, о чём раздел, и не ловит запрос; длиннее — не читается в оглавлении и в выдаче.

Раздел под одним H2 вместе с подразделами — 1000–1600 знаков без пробелов. Заголовок, под которым не наберётся и 400, — не заголовок: объедини его с соседним или сделай пунктом списка внутри раздела.

ИЕРАРХИЯ
H3 раскрывает тему своего H2 — это её часть, а не соседняя тема. H4 так же раскрывает свой H3.
Подзаголовок, который начинает новую тему, а не углубляет верхний, — это H2, и стоять он должен на своём уровне.
H3 заводится там, где в разделе больше двух абзацев прозы или два самостоятельных смысловых куска. Это норма, а не исключение: у большинства H2 есть один-два H3.
H3 не пересказывает название H2 другими словами и не заводится под каждый разряд, документ или этап — такое содержимое идёт таблицей или списком внутри раздела.

Разряды, категории и классы — одна таблица внутри раздела, а не шесть заголовков подряд. Близкие профессии, должности и форматы обучения тоже сравниваются одной таблицей.
Если статья сравнивает две профессии («X и Y: в чём разница», «X или Y»), заложи таблицу сравнения вторым или третьим разделом — она главная и стоит выше остальных таблиц. Разбор каждой профессии идёт после неё отдельными H2, а не вместо неё.

РАЗДЕЛЫ, КОТОРЫЕ ЗАКЛАДЫВАЕТ СТРУКТУРА

Таблица в статье одна, но разделы, где читатель ждёт разбора, структура обязана предусмотреть. Нет раздела — автору некуда поставить содержание, и статья выйдет сплошной прозой.

- Ступени профессии — если у неё есть разряды, категории, классы или группы допуска. Один H2 на все ступени, и внутри него таблица. H3 под отдельный разряд не заводится: «Обязанности 3-го разряда» и «Обязанности 4-го разряда» двумя заголовками — это разваленная таблица, а не структура.
- Плюсы и минусы профессии — отдельный H2: читатель выбирает профессию по нему. Внутри два подраздела, «Преимущества» и «Недостатки», каждый списком.
- Заработок — отдельный H2 с вилками по региону и ступеням.

Эти разделы в структуре есть всегда, когда тема их допускает, и считаются в общие 20–30 заголовков наравне с остальными.

ЧТО ЧИТАТЕЛЬ ИЩЕТ ПО ЭТОЙ ТЕМЕ

Возьми из списка то, что подходит теме: это ориентир, а не обязательный набор. Заголовки формулируй под профессию. Соседние пункты списка, если они об одном, объединяй в один H2 — их здесь больше, чем разделов в статье.

- кто это и чем занимается;
- должностные обязанности;
- что должен знать и уметь;
- как стать: от обучения до трудоустройства;
- какое образование нужно, нужен ли диплом или удостоверение;
- где и сколько учиться;
- сколько зарабатывает;
- разряды, если они у профессии есть;
- какие документы нужны для работы и какие выдают после обучения.

КАК РАБОТАТЬ СО СТРУКТУРОЙ КОНКУРЕНТОВ

Структура конкурентов — источник тем, а не список разделов. Их нельзя объединять: у пяти конкурентов наберётся сорок заголовков, а нужно 20–30.

Бери самые популярные и самые важные заголовки — и только их.

Популярный — тот, что встречается у большинства конкурентов: раз его завели все, читатель этот вопрос действительно задаёт.
Важный — тот, без которого не ответить на вопрос из названия статьи, даже если у конкурентов его нет.

Всё остальное отбрасывай. Тему, которая есть у одного конкурента или упомянута вскользь, в отдельный заголовок не выноси — ей место внутри соседнего раздела, списком или строкой таблицы. Заголовок, который не популярен и не важен, не заводи вовсе: он растянет статью и не добавит ответа.

Заголовки должны читаться как оглавление: по одним H2 читатель обязан понять, о чём статья и найдёт ли он в ней свой ответ. Если по заголовкам это непонятно — переформулируй их, а не добавляй новые.

ФОРМАТ ОТВЕТА

Обязательно выводи структуру именно в формате:

- H1:
- H2:
- H3:
- H4:

Никогда не используй Markdown-заголовки (#, ##, ###), только текстовые обозначения H1:, H2:, H3:, H4:.

Структура должна быть готова для копирования одним кликом в кодовом блоке. Не добавляй пояснений, комментариев или рекомендаций после структуры.

В конце структуры обязательно добавь два последних H2 — они одинаковы у всех статей и пишутся этими словами дословно:

H2: Часто задаваемые вопросы (FAQ)
H3: три вопроса, каждый отдельным подзаголовком
H2: Коротко о главном

Вопросы формулируй под тему статьи — те, что человек задаёт после чтения, а не те, на которые уже ответили разделы выше. «Коротко о главном» подзаголовков не имеет: внутри него список из 4–5 пунктов, и его пишет автор текста.

Раздела с призывом к обучению в структуре нет. Блок с приглашением, кнопкой и плашками ставится под статьёй автоматически, и заголовки вида «Запишитесь на обучение…», «Как получить…», «Пройти обучение…», «Заключение», «Итоги» в структуре запрещены: они дублировали бы этот блок.

Перед выводом пересчитай заголовки всех уровней. Больше 30 — объединяй соседние разделы об одном. Меньше 20 — тема раскрыта не полностью: посмотри, какие вопросы читателя остались без своего раздела и где кусок текста перевалит за 300 слов без подзаголовка. Заголовков ровно столько, сколько нужно на ответ: глубину даёт содержание раздела, а не их число.

Структура конкурентов:

{{.Structure}}
